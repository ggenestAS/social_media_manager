# <Brand> — Design System Reference

> The organic-post skill copies the CSS here verbatim into every post. Define
> the brand's full visual contract: palette, typography, shared CSS, signatures.

## Color palette

```
--<token>: #______;   /* role */
```

## Typography

Fonts to load in every HTML `<head>` (e.g. Google Fonts `<link>`), and a
role → font/weight/style table.

## Shared CSS block

```css
:root { /* color tokens */ }
/* backgrounds, slide containers, dimensions, reusable components */
```

## Visual signatures

The 3–5 non-negotiable visual moves that make the brand instantly recognizable.

## HTML scaffold

A minimal `<!DOCTYPE html>` skeleton with fonts loaded and the shared CSS pasted,
ready for slide markup.
