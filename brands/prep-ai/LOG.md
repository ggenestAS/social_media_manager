# prep-ai — decision log

Same rules as the sibling brand's log: one entry per belief-changing event,
newest first, observed → decided → what would change our mind → links.

---

## 2026-07-16 · LAUNCH — brand setup + EXP-2026-07-16-meta-angles staged

Brand folder created (paid-first: no social accounts yet, channels.json holds
placeholders). First experiment staged:
[`output/paid/campaigns/2026-07-16-meta-angles/`](output/paid/campaigns/2026-07-16-meta-angles/)
— 4 fake-chat static ads (feed+story), one Meta ad set per ANGLE
(bac / parents / mention / bac-blanc), destinations = the matching `-chat`
LPs so `funnel.lead.src` gives per-angle cost-per-account with no pixel.

**Decided:** angle is the first variable; LP style (editorial vs chat) is
deliberately deferred to EXP-2 on the winning angle only. Hypothesis: parents
cheapest account, mention second.
**Would change our mind:** if clicks are cheap but accounts ≈ 0 across ALL
angles, the problem is the LP/sign-up, not the angle — pause angle testing
and fix the funnel first.
**Blocked on:** a Meta ad account + page for the brand (upload is manual per
`campaign.md` checklist); social handles not yet created.
