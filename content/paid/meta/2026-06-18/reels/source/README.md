Reel HTML sources live in [`../../design-export/project/`](../../design-export/project/).

Use the standalone exports for capture (`npm run capture:reels`):

- `Albert Prep - Reels Variantes (standalone).html` — two variants (default)
- `Albert Prep - Reels Animees (standalone).html` — alternate set

The `.dc.html` files are design-canvas sources; keep them in the bundle but do not point capture at them directly.
